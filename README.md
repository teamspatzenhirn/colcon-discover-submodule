# colcon-discover-submodule
Colcon extension to update submodules before discovering packages.

This extension uses the `PackageDiscoveryExtensionPoint` to update all submodules in the current git project.

